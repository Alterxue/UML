#ifndef ROLE_H
    #define ROLE_H
    // Cet enum représente les différents rôles que les utilisateurs peuvent avoir.
    enum Role {
        PRIVATE_USER,
        PROVIDER,
        GOVERNMENT_AGENCY,
    };

#endif